import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.neuromitra.com',
  appName: 'Neuro Mitra',
  webDir: '.output/public',
};

export default config;
